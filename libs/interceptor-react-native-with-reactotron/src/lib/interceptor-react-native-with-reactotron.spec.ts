import { interceptorReactNativeWithReactotron } from './interceptor-react-native-with-reactotron';

describe('interceptorReactNativeWithReactotron', () => {
  it('should work', () => {
    expect(interceptorReactNativeWithReactotron()).toEqual(
      'interceptor-react-native-with-reactotron'
    );
  });
});
